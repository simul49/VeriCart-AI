# 2026-07-01 — Development Session

## Summary
Read all 25 PRD documents and audited the complete codebase. Identified gaps and built missing features.

## New Features Built

### Frontend (5 new/updated files)
1. **Recommendations.vue** — AI-powered product recommendation page (`/recommendations`)
   - User enters preferences in natural language
   - AI analyzes catalog and returns personalized picks
   - Shows AI explanation of why products were recommended
   - Trending products fallback section
   
2. **Wishlist.vue** — Full wishlist page (`/wishlist`)
   - View all wishlist items with product cards
   - Remove items from wishlist
   - Empty state with CTA to browse products

3. **Admin.vue** — Enhanced admin dashboard with 4 tabs:
   - Flagged Reviews (existing)
   - Orders Management (new — filter by status, update status)
   - Products Table (new — all products with trust scores)
   - Users Table (new — all users with roles)

4. **ProductDetail.vue** — Updated:
   - Integrated ChatPanel inline with "Ask AI About This Product" section
   - Wishlist button now uses real API (add/remove with check)
   
5. **ChatPanel.vue** — Updated to support general chat (no productId required)

6. **Home.vue** — Added "Top Trusted Products" section + AI Recommendations CTA

7. **ProductCard.vue** — Added `actions` slot for extensibility

8. **api/index.js** — Added `wishlistApi`, `aiApi.recommend()`, `adminApi.products()`, `adminApi.users()`

9. **router/index.js** — Added `/recommendations` and `/wishlist` routes

10. **App.vue** — Navbar now shows "AI Picks" and "Wishlist" links

### Backend (7 new/updated files)
1. **AiController.java** — Added `POST /api/ai/recommend` endpoint + general chat support
2. **AiService.java** — Added `recommend()`, `generalChat()` methods + recommendation ID parsing
3. **AiGateway.java** — Added `generalChat()` routing method
4. **QwenClient.java** — Added `generalChat()` method for non-product-specific conversations
5. **WishlistController.java** — NEW: Full CRUD (`GET /api/wishlist`, `POST /add`, `DELETE /{productId}`, `GET /check/{productId}`)
6. **WishlistService.java** — NEW: Business logic for wishlist operations
7. **Wishlist.java** — NEW: Wishlist entity class
8. **WishlistMapper.java** — Updated with proper entity mapping
9. **AdminController.java** — Added `GET /admin/products` and `GET /admin/users` endpoints
10. **UserMapper.java** — Added `findAll()` method
11. **ProductMapper.java** — Added paginated `findAll()` with filters

## Architecture Notes
- AI Recommendations flow: User input → AiController → AiService → AiGateway → QwenClient (recommend prompt) → Parse IDs → Return products
- Wishlist uses existing `wishlist` table from schema.sql
- ChatPanel now supports both product-specific and general chat modes
- Admin dashboard lazy-loads tab data to avoid unnecessary API calls

## Backend Java Warnings Cleanup (2026-07-01 22:16)
Fixed 7 Java warnings reported in VS Code Problems panel across 4 files:
- `AdminController.java`: removed unused `BigDecimal` import and unused `WishlistMapper` field + import
- `GlobalExceptionHandler.java`: replaced `FieldError::getDefaultMessage` method reference with null-safe lambda, removed now-unused `FieldError` import
- `JwtAuthenticationFilter.java`: added `@NonNull` annotations to `doFilterInternal` parameters to match `OncePerRequestFilter` contract
- `ProductService.java`: removed unused `PageResult` import

Verification: `mvnw compile` completed successfully and the linter reports 0 issues across the changed files.

## Project Run — H2 Database Setup (2026-07-01 21:15)
Since Docker and MySQL were not available, the project was adapted to run with H2 in MySQL compatibility mode:
- **Frontend**: Running on port 3000 (Vite dev server)
- **Backend**: Running on port 8080 (Spring Boot with H2 file database)
- **H2 Console**: Available at http://localhost:8080/h2-console (JDBC URL: jdbc:h2:file:./data/vericart, user: sa, no password)
- **Docker**: Installed via winget but daemon not started (needs first-time setup/license acceptance)
- **Maven**: Bundled wrapper at `backend\mvnw.cmd` used instead (Maven 3.9.9)

### Changes made for H2 compatibility:
1. Added H2 dependency to pom.xml
2. Created `schema-h2.sql` (H2-adapted schema with MERGE instead of ON DUPLICATE KEY, TEXT instead of JSON, VARCHAR instead of ENUM, TIMESTAMP instead of DATETIME)
3. Updated `application-dev.yml` to use H2 datasource with MySQL mode
4. Fixed `CartMapper.java` — replaced `JSON_UNQUOTE(JSON_EXTRACT(...))` with raw `p.images` (parsed in CartService)
5. Fixed `WishlistMapper.java` — `NOW()` → `CURRENT_TIMESTAMP`
6. Fixed `ProductMapper.java` — `NOW()` → `CURRENT_TIMESTAMP`, renamed duplicate `findAll()` to `findAllSimple()`
7. Updated `ProductService.java` — call `findAllSimple()` instead of `findAll()`
8. Added `extractFirstImage()` to CartService for JSON array parsing
9. Updated `SecurityConfig.java` — allowed `/h2-console/**` and added frame options for H2 console
10. Upgraded Lombok to 1.18.38 for Java 24 compatibility
11. Updated maven-compiler-plugin to 3.13.0 with `<release>17</release>`
12. Updated pom.xml properties: lombok.version=1.18.38, maven.compiler.source/target=17

## H2 Reserved Keyword Fix — `user` table (2026-07-02 11:50)
`user` is an H2 reserved keyword. Registration failed with `JdbcSQLSyntaxErrorException: Syntax error in SQL statement "SELECT * FROM [*]user"`.
- **Fix**: Escaped ALL `user` table references in `UserMapper.java` (9 occurrences) and `ReviewMapper.java` (2 JOINs) with backticks (`` `user` ``).
- Rebuilt and restarted backend successfully (PID 8020 on port 8080).
- Verified: Registration → 200 OK, Login → 200 OK. Both work correctly.
