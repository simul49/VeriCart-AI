# 2026-07-03

## Switched VeriCart Backend from H2 to MySQL
- Removed `spring.profiles.active: dev` from `application.yml` so dev profile no longer overrides MySQL datasource.
- Added `spring.sql.init` config to auto-run `schema.sql` on startup.
- Stripped H2 datasource override from `application-dev.yml`.
- Created `vericart` database in local MySQL 8.0 (root/paradox49@localhost:3306).
- Ran `schema.sql` — all 10 tables created successfully.
- Old H2 file at `backend/data/vericart.mv.db` is no longer used.
